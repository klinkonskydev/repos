import styled from "styled-components";

export const ThemeContainer = styled.div`
    margin: 0 auto;
    position: absolute;
    top: 5%;
    left: 50%;
    transform: translate(-50%, -50%);
    
    width: 50px;
    height: 50px;

    z-index: 2;

    cursor: pointer;
`;